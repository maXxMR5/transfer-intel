# European Transfer Intelligence

Static GitHub Pages site generated from the quality-checked Big Five transfer warehouse.

## Publish
Upload all files and folders in this directory to the root of the `transfer-intelligence` GitHub repository, then enable GitHub Pages from the `main` branch and `/ (root)`.
