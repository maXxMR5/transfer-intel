# RC1 validation report

All release checks passed before packaging:

- JavaScript syntax check passed.
- All four JSON payloads parse successfully.
- 2,508 eligible bidirectional routes remain after filtering.
- 592 eligible clubs are available in the route explorer.
- 100 routes involving non-club states were excluded.
- No eligible route contains `Without Club`, `Retired`, `Career break`, `Unknown` or `---`.
- Route ranks are continuous after filtering.
- Every route has at least one movement in each direction.
- Every route total equals its two directional counts.
- The leading eligible route remains Udinese Calcio ↔ Watford FC with 50 movements.
